<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanabelalkhayr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.VolunteerMain"></activity>
        <activity android:name=".activities.DonorMain" />
        <activity android:name=".activities.AdminMain" />
        <activity android:name=".activities.Splash" />
        <activity android:name=".activities.UpdateProfile" />
        <activity android:name=".activities.Profile" />
        <activity android:name=".activities.SendProblemReport" />
        <activity android:name=".activities.Login" />

        <activity android:name=".activities.UserMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>